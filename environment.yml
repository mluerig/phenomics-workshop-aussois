## install with `mamba env create -f environment.yaml`
name: phenomics-cv2
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - spyder
  - pip 
  - pip:
    - ultralytics
    - CLIP
    - bioencoder
    - phenopype
    - phenopype-plugins
    - jupyter
    - matplotlib
